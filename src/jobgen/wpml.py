"""Build template.kml and assemble .kmz — WPML Phase 6.

DO NOT implement until fixtures/FIXTURE_NOTES.md is complete and
tests/test_wpml.py validates the expected schema (Phase 0 gate).

Key constants derived from fixtures/FIXTURE_NOTES.md:
  WPML_NAMESPACE  = "http://www.dji.com/wpmz/1.0.6"
  DRONE_ENUM      = 77
  DRONE_SUB_ENUM  = 0
  PAYLOAD_ENUM    = 68
  PAYLOAD_SUB_ENUM = 3
  PAYLOAD_POS_IDX = 0
  TEMPLATE_TYPE   = "mapping2d"
  KMZ_SUBDIR      = "wpmz"   # files go inside wpmz/ within the ZIP
  COORD_ORDER     = "lon,lat,alt"
  HEIGHT_MODE     = "relativeToStartPoint"  # change to followTerrain for terrain-follow
"""

# Phase 6 — stub only.
raise NotImplementedError("Phase 6: wpml.py not yet implemented")
